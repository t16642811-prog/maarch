export interface UserStampInterface {
    // Stamp identifier
    id: number;

    // User identifier
    userId: number;

    // Stamp title
    title: string;

    // Content stamp url
    contentUrl: string;
}